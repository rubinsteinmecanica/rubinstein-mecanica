export type Car = {
    id: string;
    image_url: string;
};

export type Carousel = {
    id: string;
    imageUrl: string;
    title: string;
    description?: string;
};

export type Services = {
    id: string;
    name: string;
    description: string;
    image_url: string;
}